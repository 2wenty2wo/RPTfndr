import { WORDING } from '../../app/wording';

export function privacyScreen(): string {
  return `<section class="screen" aria-labelledby="privacy-title"><div class="screen-header"><div><span class="eyebrow">Web-hosted, device-local data</span><h1 id="privacy-title">Privacy and safety</h1><p>An HTTPS web server delivers the app, but there is no account, analytics, application backend, or synchronisation service. This browser communicates directly with the companion and stores logs in origin-scoped IndexedDB.</p></div></div>
    <div class="grid two"><article class="card"><h2>What stays on this device</h2><p>Targets, raw frames, classifications, location fixes, notes, bearings, verified-observer settings, session metadata, and generated estimates remain in this browser for this site origin. Copy an ended log to another device only by exporting and importing an archive yourself; imported sessions are for review and cannot be resumed.</p></article><article class="card"><h2>Network requests</h2><p>The host serves the app shell and update files. When the map is open, the browser also requests raster tiles from OpenStreetMap. Up to 300 viewed tiles may be cached for 7 days. Tile coordinates reveal the viewed area to that service; capture works without it.</p></article><article class="card warning"><h2>${WORDING.safetyTitle}</h2><p>${WORDING.safetyBody}</p></article><article class="card"><h2>Honest limitations</h2><p>A single receiver's RSSI and SNR cannot determine coordinates. Directional bearings can create only an approximate final-approach zone. Community observer reports add a broad relative-SNR likelihood zone, but terrain, multipath, antenna orientation, radio settings, and coordinate error still require close-range searching and visual confirmation.</p></article><article class="card warning"><h2>Admin coordinates are untrusted</h2><p>Positions carried in repeater adverts or contact records are manually configured. They are hidden by default and, if you choose to display the map layer, remain unverified and excluded from every calculation.</p></article><article class="card warning"><h2>Guest observer queries transmit over radio</h2><p>Observer assist is off by default and requires explicit permission plus an independently verified anchor accurate to 250 m or better. Each cycle visits at most three observers and eight neighbour pages. A blank login that reports admin permission is refused; failed login stops for that connection, and the app never guesses or stores passwords. Query events and target-matched evidence are kept in the local session and included in manual exports.</p></article><article class="card"><h2>Foreground automation</h2><p>Browsers cannot promise continuous Bluetooth or GPS work after a page is hidden or a phone locks. Smart Wardrive therefore pauses unless the page is visible, a real Drive session is active, and the companion protocol is fully connected; commands still waiting in the app queue are cancelled.</p></article></div>
    <a class="button" href="#/diagnostics">Storage and deletion controls</a>
  </section>`;
}
